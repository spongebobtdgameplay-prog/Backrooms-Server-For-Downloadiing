V0.3.16 is a clean recovery build from the known-good V0.3.14 codebase. It removes the broken V0.3.15 clickable-panel menu changes and republishes the restored original browser-style Backrooms UI as a higher version so installed V0.3.15 clients can update forward instead of getting stuck on the bad build.
