V0.3.36 rebuilds the map and minimap renderer with sub-pixel vector walls/routes, camera-stable player placement, reachable waypoint validation, edge-clamped off-screen markers, restored minimap route tracking, and CSS-driven modern map typography.
