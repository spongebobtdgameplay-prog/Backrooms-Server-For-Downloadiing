V0.3.17 fixes the native menu to follow the real Backrooms-Offical browser CSS cascade instead of the obsolete dark StartScreen rule. The menu background is restored to the official yellow #c8bb61 presentation, the CSS converter now respects exact selectors and later scoped overrides, MenuContent is back to the real 680px width, final dark-on-yellow text and border colors are used, and the giant rectangular fake glow is removed. The active main and pause menus no longer fall back to the old pixel/arcade text path.
